spring:
  application:
    name: user-service
  config:
    # config-server 비활성화
    import: "optional:configserver:"

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false