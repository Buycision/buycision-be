spring:
  application:
    name: community-service
  config:
    # Config Server 비활성화
    import: "optional:configserver:"

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
